---
description: 交互规范 - 处理状态展示、表单、表格、导航等交互时生效（整合自 global-ui-ux.md）
globs: src/**/*.vue
alwaysApply: false
---

# 交互规范

> 继承全局规范 `~/.cursor/skills/global-ui-ux.md` 通用交互部分。

## 1. 状态处理（所有页面必须覆盖）

| 状态 | 处理方式 | 组件 |
|------|---------|------|
| **加载态** | 数据请求时展示加载指示，禁止页面空白等待 | `<a-spin>` / `<a-skeleton>` |
| **空状态** | 列表无数据时展示引导文案 | `<a-empty>` + 引导操作 |
| **错误态** | 请求失败时提示并提供重试 | `<a-result status="error">` / `message.error()` |
| **成功反馈** | 操作成功后轻提示，3秒自动消失 | `message.success()` |
| **确认操作** | 删除、下线等不可逆操作必须二次确认 | `Modal.confirm()` |

```vue
<!-- 标准加载态模式 -->
<a-spin :spinning="loading">
  <a-empty v-if="!loading && list.length === 0" description="暂无数据" />
  <div v-else>
    <!-- 内容 -->
  </div>
</a-spin>
```

## 2. 表单规范

| 规则 | 说明 |
|------|------|
| 标签位置 | 默认左对齐，宽度不足时顶部对齐 |
| 必填标识 | 红色星号 `*` |
| 校验时机 | 失焦校验 + 提交时校验 |
| 错误提示 | 字段下方红色文字，用 `<a-form-item>` 内置校验 |
| 提交按钮 | 表单未修改时禁用 |

## 3. 表格规范

| 规则 | 说明 |
|------|------|
| 分页 | 默认每页 10 条，提供 10/20/50/100 切换 |
| 排序 | 关键数值列支持点击排序 |
| 筛选 | 复杂筛选用表格上方筛选栏，简单筛选用列头 |
| 操作列 | 固定右侧，超过 3 个操作收入"更多"下拉菜单 |
| 空表格 | 显示 `<a-empty>` |
| 单元格溢出 | 使用 `ellipsis: true`，hover 显示 Tooltip |

```js
// 标准分页配置
const pagination = {
  total: data.length,
  showTotal: (total) => `共 ${total} 条`,
  showSizeChanger: true,
  pageSizeOptions: ['10', '20', '50', '100'],
  defaultPageSize: 10,
}
```

## 4. 导航与布局

| 规则 | 说明 |
|------|------|
| 整体布局 | 左侧边栏（220px/64px）+ 顶部导航（48px）+ 主内容区 |
| 侧边栏 | 支持收起/展开，收起后只显示图标 |
| 面包屑 | 二级及以上页面必须有面包屑 |
| 页面标题 | 每个页面顶部有标题 + 可选描述 |
| 内容区 padding | 24px (`--spacing-page`) |

## 5. 组件使用优先级

遇到 UI 需求时：
1. **Ant Design Vue 内置组件** → 优先使用
2. **参考 ProComponents 设计模式** → 复杂场景的交互思路，用 antdv 自行实现
3. **自定义组件** → 仅当以上两者无法满足时
