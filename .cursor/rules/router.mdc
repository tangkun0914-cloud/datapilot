---
description: 路由规范 - 修改路由或页面导航时生效
globs: src/router/**/*.js
alwaysApply: false
---

# 路由规范

## 路由结构
所有业务页面嵌套在 `AppLayout` 下：
```
/ → redirect to /home
├── /home           → Home/index.vue
├── /search         → Search/index.vue（支持 query 参数：q, sort, filters）
├── /detail/:fqn    → Detail/index.vue（fqn = fullyQualifiedName，URL 编码）
├── /topics         → Topics/index.vue
├── /mytables       → MyTables/index.vue
└── /copilot        → CopilotFull/index.vue
```

## 导航方式
```js
// 搜索页带参数
router.push({ path: '/search', query: { q: keyword, sort: 'recent' } })

// 详情页（fqn 需编码）
router.push('/detail/' + encodeURIComponent(item.fullyQualifiedName))
```

## 注意事项
- Detail 页面的 `:fqn` 参数可能包含点号（如 `hive.dwa_cn.table_name`），必须 `encodeURIComponent`
- Search 页面通过 `watch(route.query)` 响应 URL 变化，避免重复请求需做 key 去重
- 新增页面需同时更新 `menuItems` 导出（侧边栏菜单）
