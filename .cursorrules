# DataPilot 项目全局 AI 规则

每次在回答用户关于本项目的前端开发、架构设计、新增页面或组件的问题前，请**必须**遵循以下规则：

1. **全局认知**：本项目采用**领域驱动设计 (DDD)**，按业务模块（如 `DataMap` 数据地图、`Monitoring` 监控运维）进行目录隔离。
2. **路由与菜单**：新增模块、页面或路由时，请修改 `src/router/index.js`，并在其中更新对应的 `xxxMenuItems` 导出。
3. **语言**：始终使用中文（简体）回复。
4. **[强制] 复杂任务规划 (Plan)**：当用户提出包含 3 个以上步骤的复杂任务，或要求开发新模块时：
 - 必须先使用 TodoWrite 工具在 `.cursor/plans/` 目录下创建一个以任务命名的 `.plan.md` 文件。
 - 文件需包含 YAML frontmatter（含 todos 数组）和需求分析。
 - 在执行过程中，必须实时更新 todo 状态（pending -> in_progress -> completed）。
 - 任务全部完成后，将该文件移动到 `.cursor/plans/archive/` 目录下归档。

## 技术栈 (Constitution)
- **框架**: Vue 3 + Vite (Composition API + SFC)
- **UI 组件库**: Ant Design Vue 4 (`ant-design-vue`, `@ant-design/icons-vue`)
- **CSS**: Tailwind CSS v4（原子化，严禁内联 style）
- **路由**: Vue Router v4
- **状态管理**: Pinia
- **图表**: 纯 SVG + Ant Design Vue 内置组件（零额外图表依赖）

## 目录结构规范 (Domain-Driven)
本项目严格按照业务模块划分目录：
```text
src/
├── components/       # 🌟 全局公共组件 (AppLayout, PageHeader, 跨模块 Copilot 等)
├── pages/            # 页面目录
│   ├── DataMap/      # 数据地图模块 (包含 Home, Search, Detail 等及专属 components)
│   └── Monitoring/   # 监控运维模块 (包含 AlertList, NotifyStrategy 等)
├── services/         # API 服务层
│   ├── request.js    # 核心请求封装
│   ├── DataMap/      # 数据地图专属 API
│   └── Monitoring/   # 监控运维专属 API
├── mock/             # Mock 数据层 (按模块划分 DataMap/ 和 Monitoring/)
├── stores/           # 状态管理 (按模块划分)
├── router/           # 路由配置 (index.js 包含路由和菜单配置)
├── App.vue           # 根组件
└── main.js           # 入口文件
```

## 编码规范
- **模块隔离**：业务专属的组件、服务、Mock数据必须放在对应的业务模块目录下（如 `src/pages/DataMap/components/` 或 `src/services/DataMap/`），禁止污染全局 `src/components/`。
- **组件拆分**：超过 200 行的页面组件必须拆分到当前模块的 `components/` 目录。
- **数据分离**：所有 Mock 数据统一放在 `src/mock/模块名/` 目录，组件通过 props 或 service 接收数据，禁止在组件内硬编码。
- **配置驱动**：新增菜单或页面入口时，修改 `src/router/index.js`，不在 UI 组件中硬编码。
- **命名规范**：文件名 PascalCase（组件），camelCase（工具函数/服务）；CSS 类名全部使用 Tailwind。